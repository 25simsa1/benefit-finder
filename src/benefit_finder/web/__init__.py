"""Optional web interface for benefit-finder.

Importing this package requires the `web` extra (`pip install
benefit-finder[web]`) which pulls in FastAPI and uvicorn. The CLI and
the core engine never import this package.
"""
